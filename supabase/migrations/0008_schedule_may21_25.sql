-- Personal data import removed from public git history.
-- Add schedule via POST /agent or scripts/schedule-api.mjs (see AGENTS.md).
-- No-op (safe if this revision was already applied on your Supabase project).

select 1;
